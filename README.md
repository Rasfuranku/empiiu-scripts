# Empiiu Scripts

## Scripts para la automatización de tareas en Empiiu

### Scripts disponibles

- PDA: Actualizar la variable `cardsData` con el output de la creacion del PDA. En el archivo `pda.html` la variable empieza en la linea `124` y termina en la linea `147`. Todo lo que esta entre `[` y `]` es el output de la creacion del PDA.